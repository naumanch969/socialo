import React from 'react'
import { Modal } from '@mui/material'

const CreateActivity = ({ openCreateModal, setOpenCreateModal }) => {
    return (
<Modal  >
</>
        )
}

export default CreateActivity